<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 内部存储读写（旧系统需要声明） -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 安装包提取/备份：需遍历全部已安装应用（含系统应用），绕过 Android 11+ 包可见性限制 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- 内存加速：结束后台进程需此权限（否则 killBackgroundProcesses 被系统拒绝，清理 0 个） -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <queries>
        <package android:name="moe.shizuku.manager" />
        <!-- 匹配所有带启动图标的已安装应用，确保 getInstalledApplications 返回完整列表 -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiquidGlass">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ScanResultActivity"
            android:exported="false" />

        <activity
            android:name=".DuplicateActivity"
            android:exported="false" />

        <activity
            android:name=".AboutActivity"
            android:exported="false" />

        <activity
            android:name=".ToolboxActivity"
            android:exported="false" />

        <activity
            android:name=".CalculatorActivity"
            android:exported="false" />

        <activity
            android:name=".UnitConverterActivity"
            android:exported="false" />

        <activity
            android:name=".DeviceInfoActivity"
            android:exported="false" />

        <activity
            android:name=".TextToolsActivity"
            android:exported="false" />

        <activity
            android:name=".RandomActivity"
            android:exported="false" />

        <activity
            android:name=".TimestampActivity"
            android:exported="false" />

        <activity
            android:name=".PartitionActivity"
            android:exported="false" />

        <activity
            android:name=".AiAssistantActivity"
            android:exported="false" />

        <activity
            android:name=".ThemePickerActivity"
            android:exported="false" />

        <activity
            android:name=".ApkExtractActivity"
            android:exported="false" />
        <activity
            android:name=".CustomCleanActivity"
            android:exported="false" />
        <activity
            android:name=".CustomRuleEditActivity"
            android:exported="false" />
        <activity
            android:name=".MemoryBoostActivity"
            android:exported="false" />
        <activity
            android:name=".RootStatusActivity"
            android:exported="false" />
        <activity
            android:name=".PermissionCenterActivity"
            android:exported="false" />
        <activity
            android:name=".UnusedAppActivity"
            android:exported="false" />
        <activity
            android:name=".PolicyActivity"
            android:exported="false" />

        <activity
            android:name=".EnterpriseActivity"
            android:exported="false" />

        <activity
            android:name=".SourceActivity"
            android:exported="false" />

        <activity
            android:name=".CompassActivity"
            android:exported="false" />
        <activity
            android:name=".DecibelActivity"
            android:exported="false" />
        <activity
            android:name=".AltitudeActivity"
            android:exported="false" />

        <!-- Shizuku：声明此 provider 后，本应用才会出现在 Shizuku 管理器“应用”列表中并可授予 API_V23 权限 -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- 文件查看：供长按扫描结果项时调用系统查看器打开文件 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
